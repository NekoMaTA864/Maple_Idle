# 新楓之谷 WZ 檔案解密與快速讀取指南 (WZ Reading Guide)

這份文件說明了新楓之谷 `.wz` 檔案的底層結構、二進制解密原理，以及現代 64 位元用戶端與舊版 32 位元結構的差異。

---

## 一、WZ 檔案核心結構 (PKG1 Format)

MapleStory 的 `.wz` 是 Wizet 官方設計的專用打包壓縮檔，檔案開頭 60 位元組（Bytes）固定為檔頭（Header）：

| 偏移位置 (Offset) | 長度 (Bytes) | 說明 | 範例數值 |
|---|---|---|---|
| `0x00 ~ 0x03` | 4 | **Magic Header** 固定簽名 | `b'PKG1'` (`0x50 0x4B 0x47 0x31`) |
| `0x04 ~ 0x0B` | 8 (uint64) | **Data Size** 資料段總長度 | 小端序 (Little-Endian) 數值 |
| `0x0C ~ 0x0F` | 4 (uint32) | **Start Offset** 內容起始偏移 | 通常為 `60` (`0x3C`) |
| `0x10 ~ 0x3B` | 44 | **Copyright** 版權宣告字串 | `Package file v1.0 Copyright 2002 Wizet, ZMS\0` |

從 `Start Offset`（即第 60 個 Byte）開始，即為 WZ 的目錄資料段。

---

## 二、字串解密演算法 (0xAA 滾動 XOR)

WZ 內部儲存的檔案名稱、節點名稱均經過簡單的滾動 XOR 加密，解密演算法非常直觀：

### 1. 字串長度讀取 (Signed Byte)
- 讀取一個 `sbyte`（有號 8 位元整數）。
- 若數值為**負數**（ASCII 字串）：真正的長度為 `-len`（或 `256 - byte`）。
- 若數值為**正數**（Unicode 雙位元組字串）：長度為 `len`。

### 2. 滾動 XOR 解密邏輯 (Python 實作)
```python
def decode_wz_ascii(raw_bytes: bytes) -> str:
    mask = 0xAA  # 楓之谷官方起始遮罩
    result = bytearray()
    for b in raw_bytes:
        result.append(b ^ mask)
        mask = (mask + 1) & 0xFF  # 每讀一個字元，遮罩遞增 1
    return result.decode('ascii', errors='ignore')
```

**驗證範例**：
- 密文：`f1 eb c9 d5 de dd ea c8 c1 d7 d7 dd c1 df d8 d6`
- 長度 Byte：`0xf1`（`-15`，長度 15）
- 解密後：`AbyssExpedition`（深淵遠征隊）

---

## 三、32 位元舊版 vs 64 位元新版架構差異

這是尋找技能檔案時**最關鍵的概念**：

### 1. 舊版單檔架構（Legacy 32-bit，約 v220 之前）
- `Skill.wz` 是**單一龐大檔案**（通常為數百 MB 到 1~2 GB）。
- 根目錄下直接包含各職業的 `100.img`（劍士）、`200.img`（法師）、`300.img`（弓箭手）、`400.img`（盜賊）、`500.img`（海盜）...以及所有技能圖示、傷害倍率與特效貼圖。

### 2. 新版階層式拆包架構（Hierarchical 64-bit，現代新楓之谷）
- 為了避免單一檔案超過 2GB 以及優化載入速度，Nexon 將大檔拆分為：
  * **主骨架檔**（`Skill.wz`）：通常**僅 100~300 bytes**！只定義有哪幾個子資料夾（如 `Dragon`、`MobSkill`、`_Canvas` 等）。
  * **分卷資料檔**（`Skill_000.wz`、`Skill_001.wz`、`Skill_002.wz` ...）：每個檔案數百 MB，裡面才是真正的職業技能 `.img`。
  * **或子資料夾結構**：例如 `Data/Skill/` 資料夾內散落各分卷。

---

## 四、楓之谷主要職業技能代碼對照表

當您用工具解析出 `.img` 檔案時，可以對照以下官方代號：

| 檔案名稱 | 代表職業 |
|---|---|
| `000.img` | 冒險家新手技能 |
| `100.img` ~ `132.img` | 冒險家劍士（英雄 110/111/112、聖騎士 120/121/122、黑騎士 130/131/132） |
| `200.img` ~ `232.img` | 冒險家法師（火毒 210/211/212、冰雷 220/221/222、主教 230/231/232） |
| `300.img` ~ `332.img` | 冒險家弓箭手（箭神 310/311/312、神射手 320/321/322、開拓者 330/331/332） |
| `400.img` ~ `434.img` | 冒險家盜賊（夜使者 410/411/412、暗影神偷 420/421/422、影武者 430~434） |
| `500.img` ~ `532.img` | 冒險家海盜（拳霸 510/511/512、槍神 520/521/522、重砲指揮官 530~532） |
| `1000.img` ~ `1512.img` | 皇家騎士團（聖魂劍士 1100、烈焰巫師 1200、破風使者 1300、暗夜行者 1400、閃雷悍將 1500） |
| `3000.img` ~ `3712.img` | 末日反抗軍（煉獄巫師 3200、狂豹獵人 3300、機甲戰神 3500、爆拳槍神 3700） |
| `MobSkill.img` | 怪物技能（BOSS 攻擊與反彈） |

---

## 五、回家如何複製所需檔案？

若您家中的電腦安裝了新楓之谷，建議您這樣打包：

1. 前往楓之谷安裝目錄下的 `Data` 資料夾（通常為 `C:\Program Files (x86)\Gamania\MapleStory\Data\`）。
2. 找到 `Skill` 相關檔案：
   * 如果是資料夾，直接把整個 `Data\Skill\` 資料夾壓縮帶過來。
   * 如果是單檔，把 `Skill.wz` 以及同目錄下所有 `Skill_000.wz`、`Skill_001.wz` ... 等全數打包。
3. 也可以直接使用專案提供的 **`WZ檔案檢測工具.bat`** 快速檢驗檔案內容！

